type DataType = {
  metaData: {
    title: string;
    description: string;
  };
  leftComponent: {
    title: string;
    displayImage?: boolean;
    description: string;
  };
};

export type { DataType };
